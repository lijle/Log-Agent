# Kubernetes Pod Log Checklist

## Checklist

1. confirm pod restart count and recent events
2. inspect CPU and memory pressure
3. check rolling deployment window
4. compare healthy and failing pod logs
5. use trace id or request id to correlate events
